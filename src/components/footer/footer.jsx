var React = require('react');

require('./footer.scss');

module.exports = React.createClass({
    render: function () {
        return (
            <div>
                <h1>Footer</h1>
            </div>
        );
    }
});
