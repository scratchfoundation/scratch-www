var React = require('react');

require('./navigation.scss');

module.exports = React.createClass({
    render: function () {
        return (
            <div>
                <h1>Navigation</h1>
            </div>
        );
    }
});
